export class Teacher {
    constructor(
        public name: string,
        public description: string,
        public email: string,
        public birthDate: string,
        public id?: string,
    ) {
        
    }
}
